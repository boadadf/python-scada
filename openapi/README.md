# OpenAPI Export

This folder contains tooling related to exporting the OpenAPI schema
for OpenSCADA Lite.

## Exporting the OpenAPI schema

Run the following command from the project root:

```bash
python -m openapi.export_openapi
